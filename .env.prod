# Database Configuration
# For running app on host machine (outside Docker):
# DGAT_DATABASE_URL=postgres://postgres:postgres@localhost:5433/dgat

# For running app inside Docker (in docker-compose):
DGAT_DATABASE_URL=postgres://postgres:postgres@localhost:5430/dgat

# Application Configuration
DGAT_PORT=3001

# Keycloak Configuration
DGAT_KEYCLOAK_URL=http://keycloak:8080/keycloak
DGAT_KEYCLOAK_PUBLIC_URL=https://app.decidel.app/keycloak
DGAT_KEYCLOAK_REALM=digital-gap
DGAT_KEYCLOAK_CLIENT_ID=dgat-admin-client
DGAT_KEYCLOAK_CLIENT_SECRET=dev-secret
DGAT_KEYCLOAK_ADMIN_TOKEN="test token"
KEYCLOAK_SERVER=http://keycloak:8080/keycloak

# JWT Configuration
DGAT_JWT_SECRET=dev-secret

# MinIO Configuration
DGAT_MINIO_ENDPOINT=http://minio:9000
DGAT_MINIO_ACCESS_KEY=minioadmin
DGAT_MINIO_SECRET_KEY=minioadmin
DGAT_MINIO_BUCKET_NAME=reports
DGAT_MINIO_USE_SSL=false
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Backend Configuration
CORS_ORIGIN=https://app.decidel.app

# Database Configuration
POSTGRES_DB=dgat
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5430

# Keycloak Configuration
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin123

# SMTP Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=yemelechristian2@gmail.com
EMAIL_PASSWORD="strd fqah rjbk mplr"
EMAIL_FROM=noreply@dgrv.coop
EMAIL_FROM_DISPLAY_NAME="DGRV COOPERATION"
EMAIL_SSL=false
EMAIL_STARTTLS=true
EMAIL_AUTH=true

# Frontend Configuration
VITE_KEYCLOAK_URL=https://app.decidel.app/keycloak
VITE_KEYCLOAK_REALM=digital-gap
VITE_KEYCLOAK_CLIENT_ID=dgat-client
VITE_API_BASE_URL=https://app.decidel.app/backend
VITE_APP_PUBLIC_URL=https://app.decidel.app

# Keycloak Host Settings
KC_HOSTNAME=app.decidel.app
KC_HOSTNAME_URL=https://app.decidel.app/keycloak
KC_URL=https://app.decidel.app/keycloak

SERVER_DN=app.decidel.app
AWS_REGION=us-east-1


